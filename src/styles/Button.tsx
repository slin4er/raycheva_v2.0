import { FC } from 'react'
import styled from 'styled-components'
import {} from '../helpers/styleTypes'

export const Button: FC = props => {
	return <StyledButton {...props} />
}

const StyledButton = styled.button``
